import PageSkeleton from '@/components/atoms/PageSkeleton';

/** Testing Strategy page: phase diagram blocks */
export default function Loading() {
    return <PageSkeleton variant="page" cards={5} showSpinner />;
}
